/** @type {import('prettier').Config} */
const config = {
  printWidth: 100,
  endOfLine: "lf",
  semi: true,
  trailingComma: "es5",
  singleQuote: false,
  tabWidth: 2,
  useTabs: false,
};

export default config;
